package org.bananaLaba.shaderModel;

// TODO: improve the shader model XML's in such way so it would be possible to weave some uniforms, attributes ant ect.
// with dynamic value sources (cameras, lights and others). Also it would be useful to be able to express such
// connections for arbitrary bindings to IoC-container objects and functions.
