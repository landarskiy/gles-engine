package org.bananaLaba.bootstrap.graph;
// TODO: re-design graph-related interfaces. The most weak points: no adequate abstractions for trees and flow graphs,
// absence of some classic collection methods such as "clear", no flexible ways to re-use classic graph implementations
// such as the adjacency list-based one in such cases when it's necessary to add weights, node content etc.
